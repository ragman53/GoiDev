# === General ===
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS generated files
.DS_Store
Thumbs.db
desktop.ini

# Temporary files (including Vim swap files)
temp/
tmp/
*.tmp
*~
*.swp
*.swo


# === Node.js / Frontend (Vite, Svelte, TypeScript) ===
# Dependencies
/node_modules/

# Build output
/dist/
/dist-ssr/ # If doing SSR, though less common for Tauri
.svelte-kit/ # SvelteKit specific build/dev output (if using SvelteKit)
build/       # Another common build output for SvelteKit/some JS projects

# Vite specific cache
.vite/

# Misc local files (like .env.local)
*.local


# === Rust / Tauri ===
# Cargo build artifacts (very important!)
# Ignore top-level target if it's ever used, but src-tauri/target is primary
/target/
/src-tauri/target/

# Tauri generated files/folders during build/bundle
/src-tauri/gen/
/src-tauri/bin/ # Platform-specific binaries generated by Tauri
/src-tauri/bundle/

# SQLite Database & Journal (Data files should not be committed)
/src-tauri/database.sqlite
/src-tauri/*.sqlite-journal

# Tauri Mobile specific (Good to add for future-proofing if mobile is ever a target)
# /src-tauri/gen/android/
# /src-tauri/gen/ios/


# === Editor / IDE ===
# VS Code
.vscode/*
# Keep recommended extensions, workspace settings, and launch configurations
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json

# JetBrains IDEs (IntelliJ, CLion, etc.)
.idea/

# Visual Studio & Rider
*.suo
*.ntvs*
*.njsproj
*.sln
*.sluo


# === Environment Variables ===
# Exclude .env files containing secrets
.env
.env.*
# But allow example/template env files under version control
!.env.example
!.env.sample